Задача 3: Изменение строки
Какая из операций изменяют строку s?

- s[0] = 'A'
- s[:] = "
+ s.replace('a', 'A')
- никакая из вышеперчисленных

Так как строки в python3 являются не изменяемые, по моему мнению не одно из них. Но если мы создадим копию
s.replace('a', 'A') она изменяет строку, s = s.replace('a', 'A') а вот так изменилась.


Задача 4: Операции со строками
Что будет результатом этой операции str("1" + '2') * int(1 + 2)?
Без использования python3,
результатом будет: 121212.


Задача 5: Символьный тип в python3
Как называется символьный тип в Python3

- char
- varchar
+ str
- ни как не называются

Символьный тип в python3 это str.
